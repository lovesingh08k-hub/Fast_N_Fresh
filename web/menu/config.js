// Production configuration for the Fast N Fresh customer QR menu.
// Keep the QR menu and staff APK on the same production backend so they
// cannot silently read different databases/configuration.
window.FNF_CONFIG = {
  API_BASE_URL: 'https://fast-n-fresh-slh9.onrender.com/api',
  VERIFIED: true,
};
